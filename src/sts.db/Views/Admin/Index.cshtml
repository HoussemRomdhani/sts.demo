@model IEnumerable<identity.Models.ApplicationUser>

<div class="text-danger" asp-validation-summary="ModelOnly"></div>
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Administration</a></li>
        <li class="breadcrumb-item active" aria-current="page">Users</li>
    </ol>
</nav>

<div class="card">
    <div class="card-header">
        <a asp-action="Create">
            Create user
        </a>
    </div>
    <div class="card-body">
        @if (TempData["message"] != null)
        {
            <div class="alert alert-success alert-dismissible" role="alert">
                <strong>@TempData["message"]</strong>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        }
        <table class="table table-striped table-sm m-1 p-1">
            <tr>
                @*<th>First name</th>
                <th>Last name</th>*@
                <th>Email</th>
                <th></th>
            </tr>
            @foreach (var user in Model)
            {
                <tr>
                    @*<td>@user.FirstName</td>
                    <td>@user.LastName</td>*@
                    <td>@user.Email</td>
                    <td>
                        <a href="@Url.Action("Infos", "Admin", new { id = user.Id})">
                            <i class="far fa-id-badge text-info"></i>
                        </a> |
                        <a href="@Url.Action("Update", "Admin", new { id = user.Id})">
                            <i class="fas fa-user-edit text-info"></i>
                        </a> |
                        <a href="@Url.Action("Delete", "Admin", new { id = user.Id})">
                            <i class="fas fa-user-times text-danger"></i>
                        </a>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="card-footer">
        <p style="text-align:right;" class="text-info">@Model.Count() user(s)</p>
    </div>
</div>
